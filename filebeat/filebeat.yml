filebeat.inputs:
  - type: log
    enabled: true
    paths:
      - /usr/share/filebeat/logs/*.log
    # json.message_key: message
    json.keys_under_root: true
    json.add_error_key: true

output.logstash:
  hosts: ['logstash:5044']
